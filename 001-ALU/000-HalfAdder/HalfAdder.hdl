CHIP HalfAdder {
    IN a, b;
    OUT sum, carry;

    // HalfAdder() = 
    // Xor(a, b) -> sum
    // And(a, b) -> carry
    PARTS:
    Xor (a=a, b=b, out=sum);
    And (a=a, b=b, out=carry);
}
